ORION FINANCE v0.4.1 — QA E CORREÇÕES PWA

Escopo desta build
- Recalibração do dock inferior no PWA instalado do iOS.
- Remoção da dupla compensação de safe area no modo standalone.
- Dock ligeiramente maior, mantendo os três destinos Mov., Início e Planejar.
- Filtros de Conta e Categoria em Movimentações convertidos para seleção explícita via seletor nativo do sistema, em vez de alternância sequencial por toque.
- Versionamento de assets/cache atualizado para 0.4.1.

QA estrutural realizado
- JavaScript: node --check aprovado.
- Service Worker: node --check aprovado.
- IDs duplicados no HTML: nenhum encontrado.
- Referências locais do HTML verificadas.
- Arquivos do precache do Service Worker presentes.
- Referências antigas movementAccountFilterBtn / movementCategoryFilterBtn removidas.
- Funções antigas de ciclo de filtros removidas.
- Desktop, backend remoto e sincronização externa permanecem ausentes.

Limitação de teste
- O ambiente não executa um Safari/PWA físico em iPhone XR. A posição final do dock em um iPhone real deve ser confirmada visualmente no aparelho.
